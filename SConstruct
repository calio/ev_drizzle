#!ft=python


Program('ev_drizzle', 'ev_drizzle.c',
        LIBS = ['ev', 'drizzle'],
        CFLAGS='-g -Wall')

Program('ev_drizzle_server', 'ev_drizzle_server.c',
        CC='clang',
        LIBS = ['ev', 'drizzle'],
        CFLAGS='-g -Wall')
